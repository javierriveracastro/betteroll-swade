<div id="brsw-gm-actions">
    {{#each actionGroups}}
        <div>{{localize label}}</div>
        {{#each actions}}
            <div data-action-name="{{name}}" class="brsw-selectable brsw-clickable brsw-action brsw-added{{#if enable}} brsw-selected{{/if}}">
                {{localize name}}
            </div>
        {{/each}}
    {{/each}}
</div>